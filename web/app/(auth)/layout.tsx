/** Auth layout — standalone screens (signup, login, password reset, verify).
 * Deliberately no marketing Nav/Footer; the auth card centers in the viewport. */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
